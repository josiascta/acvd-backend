services:
  database:
    image: postgres:15
    container_name: meu-postgres
    environment:
      - POSTGRES_PASSWORD=senha123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always

volumes:
  pgdata: